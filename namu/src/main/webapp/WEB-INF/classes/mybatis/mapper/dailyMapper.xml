<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.forest.namu.mapper.DailyMapper">

	<insert id="insertDaily" parameterType="com.forest.namu.domain.Daily">
		INSERT INTO daily (num, userId, tagId, subject, content, regDate, hitCount, notice, nickName)
		VALUES (daily_seq.NEXTVAL, #{userId}, #{tagId}, #{subject}, #{content},#{regDate}, #{notice}, #{nickName})
	</insert>

</mapper>